{
  "name": "@inventorlab/mcp-prior-art",
  "version": "0.1.0",
  "description": "MCP server exposing prior-art search tools (PatentsView, Google Patents, arXiv, Semantic Scholar) to Claude Code.",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "inventorlab-prior-art-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node20 --format=esm --outfile=dist/index.js --banner:js=\"import { createRequire } from 'module'; const require = createRequire(import.meta.url);\"",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@google-cloud/bigquery": "^7.7.0",
    "fast-xml-parser": "^4.5.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.24.0",
    "tsx": "^4.0.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=20"
  }
}
